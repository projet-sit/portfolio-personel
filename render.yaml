services:
  - type: web
    name: portfolio-ulrich
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    domains:
      - Ulrich.dev
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
